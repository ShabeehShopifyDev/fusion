<html>
<head>
    {{ content_for_header }}
    {% section 'header' %}
    <link href="{{ 'theme.css' | asset_url }}" rel="stylesheet">
</head>
<body>
    {{ content_for_layout }}
    {% section 'cart_drawer' %}
</body>
</html>
